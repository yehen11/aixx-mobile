/*
@Author - yehenSamarasinghe
@Date - 2026/08/27
*/

import 'package:flutter/material.dart';

// Canvas Base — Deep Midnight Navy
Color canvasBase = const Color(0xFF060B19);

// Surface Cards — Slate Onyx
Color surfaceCards = const Color(0xFF020617);

// Action Highlights — Electric Neon Blue
Color actionHighlight = const Color(0xFF2F6BFF);

// Gloss Outlines — semi-transparent white border, used on cards/inputs
Color glossOutline = const Color(0x14FFFFFF); // rgba(255,255,255,0.08)

// Not specified in the design guidelines doc — sensible defaults
Color onSurfaceColor = const Color(0xFFDDE2F6); // light text on dark surfaces
Color mutedTextColor = const Color(0xFF8D90A1);  // secondary/placeholder text
Color errorColor = const Color(0xFFFF6B6B);
Color successColor = const Color(0xFF4EDEA3);

// Strict corner radius from the guidelines doc
const double kCardRadius = 12.0;